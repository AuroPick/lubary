import React from "react";
import { Paragraph } from "react-native-paper";
import { Center } from "../utils/Center";

interface NinniProps {}

export const Ninni: React.FC<NinniProps> = ({}) => {
  return (
    <Center>
      <Paragraph>Ninniler</Paragraph>
    </Center>
  );
};
